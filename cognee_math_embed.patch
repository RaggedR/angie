diff --git a/cognee/infrastructure/databases/vector/embeddings/get_embedding_engine.py b/cognee/infrastructure/databases/vector/embeddings/get_embedding_engine.py
index de56c8971..2ef8f2eea 100644
--- a/cognee/infrastructure/databases/vector/embeddings/get_embedding_engine.py
+++ b/cognee/infrastructure/databases/vector/embeddings/get_embedding_engine.py
@@ -79,6 +79,16 @@ def create_embedding_engine(
 
         Returns an instance of an embedding engine based on the specified provider.
     """
+    if embedding_provider == "math-embed":
+        from .MathEmbedEngine import MathEmbedEngine
+
+        return MathEmbedEngine(
+            model=embedding_model or "RobBobin/math-embed",
+            dimensions=embedding_dimensions or 768,
+            max_completion_tokens=embedding_max_completion_tokens,
+            batch_size=embedding_batch_size or 64,
+        )
+
     if embedding_provider == "fastembed":
         from .FastembedEmbeddingEngine import FastembedEmbeddingEngine
 
